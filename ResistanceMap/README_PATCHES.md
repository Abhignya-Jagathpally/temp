# README.md — required patches

Apply these diffs directly to `README.md` on the `v3-fixes` branch. They do not
change the mechanics of the pipeline; they fix the documentation claims that
`scripts/check_docs_consistent.py` would otherwise reject.

---

### Patch 1 — ODE solver label

```diff
 trajectory:
   ode_solver: dopri5
-  # Adams-Bashforth (higher accuracy)
+  # Dormand-Prince Runge-Kutta (4,5); adaptive step; torchdiffeq
   forecast_horizons: [3, 6, 12]
```

### Patch 2 — Remove NIST ZT claim

```diff
 Verification Layer
-  ├─ Zero-Trust: SHA256 hash chains at every agent boundary
+  ├─ Reproducibility: SHA256 content-hash chain at every agent boundary
   ├─ Guardrails: 8 built-in (probability range, IC50 bounds, stochastic, etc.)
-  ├─ First-Principles Math: Fano's inequality, Lyapunov stability, Kramers rate
+  │                        (see resistancemap/infrastructure/guardrails.py for definitions)
   └─ Data Quality: SOTA benchmark comparison (DeepSurv, DeepCDR, STRING v12)
```

Restore the "First-Principles Math" bullet only after
`resistancemap/infrastructure/info_bounds.py` wires Fano/Lyapunov/Kramers into
the `validate` stage output.

### Patch 3 — Reconcile PPI numbers

```diff
-├─ STRING PPI v12 (19,177 nodes, 930k directed edges after ENSP→gene mapping)
+├─ STRING PPI v12 (19,566 raw nodes / 11.9M raw edges;
+│                  MM subnet 7,853 nodes / 460,212 undirected edges used for training)
```

Matches `FACTS["ppi"]` and `AGENT_ORCHESTRATOR.md` §ProteinNetAgent.

### Patch 4 — Fusion hidden_dim

Pick ONE. Keep 256 in README; update `AGENT_ORCHESTRATOR.md` to match.

```diff
-  Hidden: 128-dim intermediate
+  Hidden: 256-dim intermediate  # reconciled with README + FACTS["fusion"]
```

### Patch 5 — Runtime table

```diff
-**Latest run (with patient data):** 10/10 agents, 21.3 min total,
-test_mse=0.7191 on 132 samples across 11 drugs.
+**Latest run (r-2026-04-14-smoke):** public-subsample smoke test, 10/10 stages,
+21.3 min total on 2x A100. See `RUNS.md` for the full run ledger;
+headline metrics come from `r-2026-03-02-full` (1x H100, 26.9 h, n=150 held-out).
+Controlled-access cohorts (MMRF CoMMpass) are NOT included in any publicly
+reported metric — see §4.2 of the remediation plan.
```

### Patch 6 — Performance table with baselines + CIs

```diff
-| Metric | VAE Only | + Trajectory | + Fusion | + Landscape |
-|--------|----------|--------------|---------|-------------|
-| AUROC (Resistance) | 0.82 | 0.85 | 0.88 | 0.89 |
-| AUPRC (Resistance) | 0.76 | 0.79 | 0.83 | 0.84 |
-| MAE (3-month forecast) | 0.18 | 0.12 | 0.09 | N/A |
-| Top-1 mechanism recall | — | — | 0.72 | 0.78 |
+Headline performance is reported per-drug with 95% bootstrap CIs and against
+the mandatory baseline panel (`global_mean`, `per_drug_mean`, `ridge_esm2`,
+`deepcdr`). Pooled AUROC is shown for context only; per-drug tables are the
+publishable claim. See `reports/headline_table.md` regenerated by
+`bash reproduce.sh`.
+
+Ablation rows use frozen upstream weights, 5 seeds, and paired bootstrap
+against the row above. Rows marked `n.s.` are explicitly kept in the table.
```

### Patch 7 — Agentic → DAG-scheduled

```diff
-# Agentic mode (default — DAG parallel execution)
+# DAG-scheduled mode (default — Kahn topological sort + asyncio.gather)
```

### Patch 8 — MMRF access

```diff
-| MMRF CoMMpass | Clinical | GDC Portal | 50+ GB | VCF/TSV | IRB Approval |
+| MMRF CoMMpass | Clinical | GDC Portal | 50+ GB | VCF/TSV | dbGaP DAR required; excluded from public release |
```
